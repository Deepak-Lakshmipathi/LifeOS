# LifeOS — Handoff

Last updated: 2026-08-05 (session 2). **TWO SLICES MERGED TRIPLE-GREEN, 0 rejects — #186 then #183, run serially because their write-sets both touch `App.tsx`.** Board **59 closed issues**, 2 open. First full session on the GitHub-issues tracking model; it held — `labelDrift` was empty at open and at close.

**#186 — one owner for the task list** (PR **#193** → `c2822b1`). `VitalsRow` (Completion tile) and `Aurora` (warmth tint) did not read `useTasks`; they self-loaded through `src/sync/selfLoadTasks.ts`, a module-level `inFlight ??= provider.list()` memo whose only invalidation path was marked test-only. Two owners for one list: a write through the hook updated one and left the other frozen at its first resolution for the life of the page, so **Completion and the tint only ever moved on a full reload** — before *and* after S64, which is why S64 made this its negative DoD #11. The fix deletes the second reader rather than adding an invalidation channel: both components take `tasks` as a **required prop** from `useTasks` in `App.tsx`. All 30 existing test call sites already injected `tasks`, so the entire self-load path (state, effect, module, its test) was dead once App wired the prop through. The module also held the **second process-wide `VaultSync`**, so this is a free step toward #190. Same single-owner shape ADR-0016 applied to the transport.

**#183 — capture moves off Home** (PR **#196** → `71bb530`). Owner request from live testing: Home is the check-in surface, so the write affordance does not belong on it. `+ New task` and its `CaptureSheet` bottom sheet moved from `HomeView` to `TasksView` **verbatim** (the review gate byte-diffed the block against master and confirmed only the comment changed). `HomeView` no longer takes an `onAdd` prop at all, so re-adding capture there requires changing the component's interface. **Owner decision (2026-08-05): the button sits ABOVE the `Segmented` sub-nav, so it shows on all three segments** (Tasks/Domains/Pulse) — capturing is exactly what you want to do while looking at Domains or Pulse, and an affordance that appears and disappears as you switch segments is worse than one that stays put. This reverses an explicit S58 decision, so `HomeView`'s header comment now records where capture went and is marked **do not add it back**. The old ordering rule (S71/#181 before #183) is void — #181 is cold-storage; if revived it rebases onto this.

**The gates earned their keep in two different ways, and both times it was something the implementer would have missed.**
- On **#186**, review and eval **independently** flagged that the fix had *created* a vacuous test: `Aurora.test.tsx` still asserted "injecting tasks short-circuits the provider load — the provider is never called", but Aurora no longer has a load path, so the assertion could not fail while its name advertised a guard that no longer existed. That is the **#120 failure mode**, and it was deleted rather than rewritten because the property is now structural. Two stale citation sites in `transport.test.ts` (including a "seven owners on master" arithmetic that counted a deleted call site) were fixed in the same pass.
- On **#183**, the eval gate **proved** the segment test load-bearing by mutation — moving the button inside the `tasks` conditional turns both Domains and Pulse red — and was honest that the issue's third regression clause ("a task added from Tasks reaches the provider") is only asserted against a mock `onAdd` at the prop boundary; the real path rests on `e2e/pwa.spec.ts`, which is a merge gate and passed. Called PASS with the gap stated rather than papered over.

**`pwa-e2e` flaked once and was diagnosed, not waved through.** It fired on #193's second commit — which was **comments plus one deleted vitest test**, causally incapable of affecting offline persistence, on a parent that had just passed the same job. Exact fingerprint match; reran green. **That is the 5th firing and it is now filed as #194**, with the full history — the stabilization ticket `docs/agents/afk-pipeline.md` has recommended since **2026-07-10** finally has a home.

**Two follow-ups filed, neither implemented:** **#194** (the flake above) and **#195** (Aurora's warmth tint has no *liveness* guard — its math is tested five ways, but nothing asserts it updates after a write; currently safe only because it shares one required prop with Completion, which IS pinned).

**Housekeeping:** created the **`status:in-progress` label**, which had never existed despite the state model depending on it — `state.mjs` reported `labelDrift: []` only because it cannot flag a label that isn't in the repo. Both merged issues kept `status:in-progress` after closing; cleaned by hand. Both feature branches deleted local + origin.

**🔄 PROJECT TRACKING IS GITHUB ISSUES (owner decision, 2026-08-05) — the kanban method is retired.** `kanban.html`, `lifeos-hub.html` and `scripts/build-hub.mjs` are **deleted** (recover with `git show 7a06f62:<file>`). **An issue's labels ARE its state.** `status:ready` / `status:blocked` / `status:in-progress`, `needs-triage`, and `cold-storage` (applied *with* closing — parked on purpose, reopen to revive). **Waves are gone entirely.** Dependencies are a `Blocked by: #N` line in the issue body; hotspot batching is a **by-hand write-set check**.

**❄️ 12 issues in cold storage** (#175, #177–#182, #184, #188–#191) — a deliberate parking decision, not a verdict. Every one is real and reproducible; the analysis in each thread stays valid; reopening is the revival path.

**Board: 2 open issues, both `status:ready` (#194, #195) · 12 cold-storage · 0 in-progress · 0 blocked · 0 needs-triage · 0 open PRs · 59 closed.** The historical slice count (archival) is in `## Outstanding board state`; `state.mjs`'s `counts.done` counts closed *issues* and will never match it.

**✅ Master was 0 ahead, 0 behind origin at the time of writing — this handoff commit makes it 1 ahead.** Push with a plain **`! git push origin master`** (behind is 0, so it fast-forwards; no rebase needed). Verify with `node .claude/skills/lifeos-boot/scripts/state.mjs` rather than trusting this line.

**6 untracked scratch specs still left on purpose** (`e2e/zz{diag173,repro173,repro173b,scratch-s71}.spec.ts`, `src/test/zz{repro173,scratchS71}.test.tsx`). They assert against known-open bugs and `pwa-e2e` is a merge gate, so they must NOT be committed — but `src/test/zzscratchS71.test.tsx` is the **starting harness for the S71 spike**, so don't delete them either. **#183 repointed `zzscratchS71.test.tsx` from `HomeView` to `TasksView`** — it rendered `HomeView` with `onAdd`, and once that prop was gone the file broke `tsc`, which broke `npm run build`, which killed Playwright's `webServer` and made every local e2e run die with a bare `Process from config.webServer was not able to start. Exit code: 2`. **An untracked file can break your local build, and CI will never tell you.**

> **NEXT SESSION — one HITL gate is owed first, then #194, then #195.** `/lifeos-boot` → `state.mjs` → sitrep → act. Both open issues are `status:ready`, neither is authored as a slice ticket, and their write-sets are **disjoint** (`e2e/pwa.spec.ts` vs `src/components/glass/Aurora.test.tsx`) — so they MAY run two-wide if you want the parallelism.
>
> **⛔ HITL owed: S66 DoD #12 — and the deployed build now also carries #186 and #183, so this is the moment to walk all three.** Open https://deepak-lakshmipathi.github.io/LifeOS/ with DevTools. **Hard-reload first** (Ctrl+Shift+R, or Application → Service Workers → "Update on reload") — it is a PWA and a soft refresh serves the cached old build. Then: hard-reload across **two full mount cycles including one where the remote 401s**, and confirm **zero** `Lock broken by another request` (was 7 per cold load + 1 per habit tap). Eyeball TodayCard/AttentionCard for real content rather than a confident empty state — a ghost file used to render "no events today" with no error at all. Paste filtered console output into PR #192. **No CI test can substitute:** jsdom has no `navigator.locks`, so `DefaultBackend.init` picks the IDB `Mutex`, not `Mutex2`, and the steal mechanism is absent from the test environment. A test that mocks `navigator.locks` is the #120 failure mode and must be rejected.
>
> **While you are in there, verify this session's two slices by hand:** Home has **no** `+ New task`; the Tasks tab has it **above** the sub-nav and it stays put across Domains/Pulse; completing a task via the mission dot moves the DAY REVIEW **Completion** counter **without a reload** (that is #186's whole point). The warmth tint only shifts for **domain-tagged** tasks — `computeWarmth` is domain-based, so a domain-less task not moving the tint is correct, not a bug.
>
> **#194 — `pwa-e2e` offline-persistence flake (bug, gate integrity).** 5 firings, every one on a diff that could not plausibly affect offline persistence. It fails on both the first attempt *and* Playwright's own retry, then passes on a fresh job rerun — which points at process/worker-level state (service worker, IndexedDB warm-up), not a short assertion timeout. Worth doing first because it is a **merge gate**: each firing trains whoever is on the gate to reach for "rerun, it's the known one", and that is how a real offline-persistence regression gets waved through. Suspects and where to look are in the issue body. Raising the 8000ms timeout would make it rarer, not correct.
>
> **#195 — Aurora warmth tint liveness (enhancement, defensive).** Needs a **domain-tagged** fixture chosen so a single completion produces a measurable opacity delta; a domain-less fixture would pass whether or not the wiring worked, which is exactly why #186 did not just bolt an assertion onto its own test. Reuse the App-level harness in `src/test/completionLiveUpdate.test.tsx`. Keep `Aurora.test.tsx`'s existing §7/§8 cap assertion (`alpha <= 0.1`).
>
> **⚠️ Known, unticketed, pre-existing: the capture sheet has no `role="dialog"`/`aria-modal`, no focus trap, no Escape-to-close, and no focus return to the trigger on close.** Unchanged by #183 — it moved with the markup — but it now sits on the app's **only** capture surface, which raises the stakes. Flagged by the #196 review gate. File it if you want it fixed; it is not currently tracked.
>
> **⚠️ 3 unit tests fail LOCALLY and are not real.** `src/vault/transport.test.ts` asserts `VITE_VAULT_REPO_URL is not configured` rejects; this machine's gitignored `.env` sets it, so they fail here and pass on CI. Confirmed by deleting `.env` in a scratch worktree (that file goes green). Expect **1208 passed / 3 failed** locally on a clean tree — do not chase them.
>
> **⚠️ `graphify-out/` is now THREE SLICES STALE — it predates S66, #186 and #183.** Last full rebuild 2026-08-04 (`d855e3f`: 1677 nodes · 3233 edges · 108 labelled communities · 21 hyperedges, from 354 files). This close **deliberately skipped** the refresh again: `--update` is broken here (below) and a full rebuild is a 7-subagent job needing owner approval — not worth it for this delta. **What the graph does not know:** `getVaultTransport()`, ADR-0016, the in-place reset, the 6 call sites that stopped constructing their own transport, **the deletion of `src/sync/selfLoadTasks.ts`, the new `src/test/completionLiveUpdate.test.tsx`, and capture's move from `HomeView` to `TasksView`.** Fold it into the next rebuild rather than running one just for this. **Do NOT trust `/graphify --update` here** — `detect_incremental` marks all ~354 files changed AND the semantic cache returns ~1 node per file, which is how a previous attempt came out at 1577 against the then-current 2747 and tripped graphify's own overwrite guard. **Run full rebuilds until that is fixed**, bypass the semantic cache, and never force the guard without inspecting the *composition* of what you'd lose (the 2747 → 1677 drop was a quality win: 546 slice-section headings and 100 JSON field names out, concept+rationale 7% → 15%).
>
> **Dispatch note:** merging needs triple-green = CI (**every job**, incl `pwa-e2e`) + ponytail-review (ultra) + a **FRESH** eval subagent vs the numbered DoD. Standing local gate: run **`npm run build`** (tsc), not just `npm test`. `[UI]` work: feed `docs/DESIGN_LANGUAGE.md`, tokens-only, honor reduced-motion via `useReducedMotion()`, don't break `data-testid="tab-bar"`. Telegram bot's owner-only S16c live verify (`afk-pipeline-out/s16c-verify-checklist.md`) remains a standing human gate.

**Session lessons (2026-08-05 session 2, #186 + #183 — serial, shared `App.tsx` lane) —**
- **A fix can CREATE a vacuous test, and the author is the last person to notice.** #186 deleted Aurora's provider load path; `Aurora.test.tsx` kept asserting "the provider is never called under test" — an assertion that could no longer fail, still advertising a guard by name. Both gates caught it independently; neither the implementer nor the local suite could, because the test was *green*. Apply: when a change deletes a code path, grep the test names that describe that path. A green test whose subject no longer exists is worse than no test — it reads as coverage. Delete it rather than rewriting when the property has become structural (a required prop, a type) rather than behavioural.
- **"Delete the second reader" beats "add an invalidation channel."** #186's stale-cache bug had an obvious fix (expose the reset, call it on write) and a smaller one (make the list a required prop from the single owner). The smaller one removed 226 lines, killed a second `VaultSync` instance for free, and made the bug **structurally** unreachable rather than correctly handled. Apply: when a cache is stale, first ask whether it should exist. Two owners of one piece of state is the defect; synchronising them is treating the symptom.
- **Check that a label your state model depends on actually exists.** `status:in-progress` was in `CLAUDE.md`, both skills, and the boot/close docs — but had never been created in the repo, so `gh issue edit --add-label` failed on first use. `state.mjs` reported `labelDrift: []` throughout, because it can only compare labels that exist. Apply: a drift-detector that reads from the same vocabulary it validates cannot see a missing term. Run `gh label list` against the documented set once, not per-session.
- **An untracked file can break your local build, and CI will never tell you.** `zzscratchS71.test.tsx` (untracked, deliberately kept) rendered `HomeView` with a prop #183 removed, so `tsc` failed → `npm run build` failed → Playwright's `webServer` died → every local e2e run reported `Process from config.webServer was not able to start. Exit code: 2`, which names nothing about the real cause. Apply: when local e2e dies at webServer startup, run `npm run build` directly — and keep deliberately-untracked harnesses compiling, or they become a tripwire on unrelated work.
- **A flake fingerprint is a hypothesis; causality is the proof.** `pwa-e2e` fired on a **comments-only** commit whose parent had just passed the same job. The fingerprint matched, but what actually settled it was that the diff was causally incapable of affecting offline persistence. Apply: match the signature *and* state why the change could not have caused it. This one has now fired 5× and was finally filed (#194) instead of rerun again — the rerun habit is itself the risk, because it is how a real regression eventually gets waved through.
- **An eval gate that reports a coverage gap it decided not to block on is doing its job.** #183's eval called PASS while stating plainly that "reaches the provider" is only asserted against a mock at the prop boundary, with the real path resting on e2e. Apply: prefer a PASS with a named gap over a silent PASS or a reflexive FAIL — the gap is the thing the next session needs, and burying it in a verdict loses it.
- **Distillation done this close** — the 2026-08-03 block was folded into the standing `## Lessons / gotchas` list. Dated blocks in the head are back to **3** (this one, 08-05 s1, 08-04); fold the oldest when they reach 4.

**Session lessons (2026-08-05) —**
- **A test that asserts a count went DOWN can pass on broken code for the opposite reason.** The naive `Promise.all` form of S66's "two consumers → one `git.pull`" would have been green on master: a Vitest race between concurrent dynamic `import()`s of a `vi.mock`-ed specifier let one caller reach the real `isomorphic-git`, die on `ENOTFOUND`, and leave the survivor reporting the exact `pull === 1` the fix was meant to produce. Apply: when a test proves work was *collapsed*, enumerate what else could lower the number — a crashed participant is indistinguishable from a collapsed one unless you make the overlap explicit (hold reader A inside the call before B starts).
- **A pre-resolved ticket can specify a DoD its own write-set cannot satisfy.** S66 DoD #6 required `VaultSync.list()` to survive non-string content, but its test injects a fake transport, so the transport-level guard the write-set allowed could never be reached. Apply: at authoring time, trace each DoD's *test seam* back to the files the write-set permits; if the test bypasses them, the write-set is wrong.
- **Check an ADR number is free before a ticket names one.** Owner decision 7 said "ADR-0011"; 0011–0015 had been Accepted since S16. Written from memory of the decision log, never checked. Apply: `ls docs/adr/` at authoring time — it costs one command and the implementer otherwise has to make the call mid-slice.
- **A close banner asserting "pushed" needs an ahead-count, and `state.mjs` has none.** It reports `behindOriginMaster` only, so last session's own handoff commit sat unpushed under a "0 ahead, 0 behind" banner and the next pull made a merge commit. Apply: until the script is fixed, run `git fetch origin && git rev-list --left-right --count master...origin/master` by hand at close — and fix the script.
- **One board is better than two, and zero local boards is better than one.** `kanban.html` had to be hand-edited on every slice, could silently disagree with GitHub, and fed a hub the owner never opened. Retired 2026-08-05 for issues + labels. Apply: state belongs where the work already is; a tracker that needs a separate write step will drift from reality exactly as often as someone forgets it.
- **When you retire a tracking artifact, grep for its readers before deleting.** `kanban.html` was read by `state.mjs`, `build-hub.mjs`, both LifeOS skills, `CLAUDE.md`, `triage-labels.md`, `afk-pipeline.md` and `docs/slices/README.md` — deleting the file alone would have left seven dangling references and a broken boot. Apply: the file is the easy part; its consumers are the change.
- **Distillation due next close** — dated session-lesson blocks in the head are now at **3**; fold the oldest into the standing `## Lessons / gotchas` list when they reach 4. Six operational lessons from this run are in `afk-pipeline-out/LESSONS.md` and the manifest `afk-pipeline-out/s66-vault-fs-single-owner-run.json`, not duplicated here.

**Session lessons (2026-08-04) —**
- **A pre-resolved, owner-sanctioned ticket can still be wrong — and its own tests can hide it.** DoD #7's prose and its test (e2) specified different state machines; every test stopped at the first op's settle, so neither model could be falsified by the suite. Apply: when a ticket describes a state machine, write one test that runs it to **rest** and compares against the *external* system (a stateful mock), not just the intermediate UI. "Assert where it lands, not just what it does on the way."
- **A DoD item that says "proven by tests" needs the grep, not the reading.** `deleteTask` was implemented correctly and had zero coverage; the code looked right, so nobody looked for the test. Apply: for every "proven by tests" criterion, grep the test file for the *symbol* before calling it PASS — that is exactly what the eval subagent did.
- **Check a "known flake" hardest when the diff touches its path.** #187's red matched a recorded fingerprint, but the PR changed `addTask`'s write path and the test adds-then-reloads. What settled it was history, not pattern-match: the same signature failed twice on **docs-only** diffs in July. Apply: a fingerprint match is a hypothesis; the proof is a rerun plus evidence the flake predates the change.
- **A tooling guard that measures a proxy can be right to fire and wrong to obey.** graphify refused to overwrite a 2747-node graph with a 1677-node one; the refusal was correct on the incremental run (broken cache) and wrong on the full rebuild (the surplus was markdown headings and JSON field names). Apply: when a guard blocks on a COUNT, inspect the composition of what you'd lose before either honoring or forcing it — four graphify lessons are in `afk-pipeline-out/LESSONS.md`, not duplicated here.
- **Distillation due next close** — dated session-lesson blocks in the head are now at **2**; fold the oldest into the standing `## Lessons / gotchas` list when they reach 4.

**— previous session —**

Last updated: 2026-07-31 (session 2). **Post-v2 WAVE 13 COMPLETE — S59 + S62 merged triple-green, dispatched two-wide in parallel worktrees.** Shipped: **S59** (#169 → `e07b6d7`) tab bar fits every screen — `TabBar.tsx` buttons went fluid (`px-[clamp(6px,2.5vw,20px)]` / `text-[clamp(11px,1.75vw,14px)]`, track `w-max max-w-full`), both clamps saturating at **800px** so ≥841px is pixel-identical to S58; measured in real Chromium at 320/360/390/1280px · **S62** (#170 → `84203c2`, closes **#155**) `GitTransport.loadGit()` now does its synchronous `if (!url)` check **above** the dynamic `import()`, so the unconfigured path never loads isomorphic-git/lightning-fs; every `import.meta.env.VITE_VAULT_REPO_URL` read is gone from `src/components/` (zero hits repo-wide outside `App.tsx`/`selfLoadTasks.ts`/`pat.ts`/`transport.ts`).

**Both PRs needed a gate-caught revision before merge — the gates keep paying for themselves, do not shortcut them:**
- **S59**: the author shipped a *modelled* guarantee where DoD 1 asked for a *measured* one — a jsdom box model with `SHELL_GUTTER_PX`/`TRACK_CHROME_PX` hand-transcribed from `App.tsx` (jsdom has no layout engine and drops `clamp()` from its CSSOM). Fixed by adding the real check to `e2e/pwa.spec.ts` (`scrollWidth <= clientWidth` + no document h-scroll + full labels, 4 widths, ~3s), **verified red** against a revert to the fixed metrics. The eval also caught the `max-w-full` comment claiming containment it does not provide — it caps the frosted track box, NOT the button row; the clamps alone deliver the fit.
- **S62**: passed all 6 numbered DoD and still carried a measured regression *on the very path the slice exists to make cheap*. `getVaultPat()` (`src/vault/pat.ts`) falls through to a **blocking `window.prompt`** when no PAT is stored, and it was still called above the new guard — with the per-card short-circuits deleted, 3 more mounts reached it: `cockpitShell` prompts **10 → 25**, full suite 38 → 99, i.e. **5 sequential PAT modals per Home mount** on an unconfigured browser build, up from 2. One-line move below the throw → **25 → 0**.

**#172 merged by the owner** (`25476fd`, closes **#167** + **#168**) — `fix(sync): selfLoadTasks memo`, i.e. `inFlight ??= provider.list().catch(err => { inFlight = null; throw err })` + a `__resetSelfLoadTasksCache()` seam. #167 was the rejected promise cached forever, degrading the Completion vital + warmth tint for a whole session after one transient read error. **Merged on 2-of-3 green by owner decision, not on a full triple-green:** its eval subagent died on a monthly spend-limit API error before doing any work, so the third green is the dispatcher-run verification recorded in the PR comment — CI ×3, mutation red on exactly the retry assertion, full suite 1186 pass / exit 0, `cockpitShell.test.tsx` blob byte-identical to master, both callers `.catch()`, no reachable ABA race, `GitTransport.inflight` confirmed to lack the same bug shape. Worth a fresh eval on a later pass if the memo ever misbehaves.

**Also landed this session (repo hygiene, both from real time lost):** `docs/agents/afk-pipeline.md` contradicted itself on pwa-e2e — line 13 said e2e "is NOT a slice acceptance target" while the gate list named only `build-test`/`bot-test`, and a wave-13 author quoted the lenient half to excuse local e2e reds. Doc now states vitest is the default acceptance surface, e2e is legitimate when jsdom structurally cannot observe the property (S59 precedent), and **every CI job must be green**. And `playwright.config.ts` `webServer.timeout` 60s → **180s**: that budget spans `npm run build && npm run preview`, so under parallel agent load the build alone blew it and the run died with `Timed out waiting 60000ms from config.webServer` — infra noise that reads as an app failure and got misreported as flake twice this wave.

**Board: 74 done, 0 ready, 0 progress, 0 backlog — the post-v2 wave is fully drained. Open issues: 0. Open PRs: 0.**

**✅ Master is 0 ahead, 0 behind — in sync, tree clean, nothing owed** (owner pushed the close-out commit `d058fc6`; this banner's own commit is the only thing that can be ahead when you read it). The owner pushed mid-session (`25476fd..5613f3b`) after merging #172 on origin. Note for next time: the harness DENIES `git push origin master` to the agent even though the owner granted standing permission — feature-branch pushes (`git push origin HEAD:<branch>`) go through fine. Hand the owner `! git push origin master` and keep working; don't let docs commits pile up on local `master`, because worktrees share local refs and a dispatched agent branching off local `master` inherits them (that produced the 8958-line PR #171, deleted and rebuilt as #172).

**✅ Housekeeping DONE — worktrees and branches fully cleaned this session.** 13 worktrees → **1** (the repo), 56 local branches → **23**, 3 orphan dirs → 0. Removed: 9 in-repo agent worktrees, the 3 external eval trees (`../LifeOS-eval-*`), 6 merged slice branches, 33 empty `worktree-agent-*` harness branches, 3 empty orphan directories. `.claude/worktrees/` is empty. The 23 remaining branches are older merged work (`afk/*`, `slice/*`, `chore/*`, `docs/*`, `s20-*`, `feat/s14-vault-read`) — pre-existing, deliberately left.

> **NEXT SESSION — LIVE TESTING, driven from the Claude Code session itself.** Owner-chosen focus. The board is empty (74 done / 0 unblocked / 0 open issues / 0 open PRs), so there is nothing to dispatch — and live testing is the *right* work while the spend limit is unresolved, because it needs **zero subagents**. Drive the deployed PWA with the `claude-in-chrome` browser tools, exercise it as real usage, and file every defect as a GitHub issue.
>
> **Target:** `https://deepak-lakshmipathi.github.io/LifeOS/` (Pages build; `VITE_VAULT: '1'` + `VITE_VAULT_REPO_URL` → `LiveOS-VaultRepo` are baked in at build time — see `.github/workflows/deploy-pages.yml:31-36`).
>
> **⚠️ READ FIRST — the PAT prompt will freeze browser automation.** The hosted build is vault-*configured*, so the first vault read calls `getVaultPat()` (`src/vault/pat.ts:26`), which raises a **blocking `window.prompt`** when `localStorage.lifeos_vault_pat` is empty. A modal dialog blocks every subsequent browser event and the extension goes unresponsive — the session is stuck until it's dismissed by hand. Seed the token BEFORE the app ever mounts:
> 1. Navigate to a same-origin, non-app URL first — e.g. `https://deepak-lakshmipathi.github.io/LifeOS/manifest.webmanifest` (same origin, no app JS, so nothing prompts).
> 2. `javascript_tool`: `localStorage.setItem('lifeos_vault_pat', '<fine-grained PAT, vault repo, Contents R+W>')`.
> 3. Only then navigate to the app root.
>
> Without a PAT the app still renders — every self-load `try`/`catch`es into its honest empty state — so testing the UI *without* vault data is a valid pass too; just dismiss the one prompt manually and expect empty vault surfaces.
>
> **What to exercise (all shipped but never seen live by a human):**
> - **S59** responsive tab bar — resize to **320px** and confirm all five labels (Home·Tasks·Money·Career·Agents) are reachable and unclipped. `e2e/pwa.spec.ts` asserts this in Chromium, but the owner's real phone is the point.
> - **S58** Tasks tab — Domains/Pulse now live as `Segmented` sub-nav inside Tasks, and Home is slimmed to the check-in surface.
> - **S60** warmth as background tint (`Aurora.tsx`) + the Completion vital in the first tile slot.
> - **S61** FleetStrip + the daily brief — **these were shipped-but-dead in production** until the recursive `_readFiles()` landed; this is the first session that can confirm `agents/<name>/status.json` and `Briefs/<date>.md` actually reach the live app. Highest-value thing to look at.
> - The **v1 user-testing checklist** lived in the "Get started & test" tab of `lifeos-hub.html`, deleted 2026-08-05 — recover it with `git show 7a06f62:lifeos-hub.html` if you want to walk it.
>
> **Browser-tool discipline:** load every `mcp__claude-in-chrome__*` tool you expect to need in ONE `ToolSearch` call; never click anything that raises an `alert`/`confirm`/`prompt`; read errors with `read_console_messages` (filter by pattern) and `read_network_requests` rather than guessing. File findings with `/triage` → `needs-triage`.
>
> **After live testing, when the limit is raised:** file findings as GitHub issues and cut tickets into `docs/slices/` (the post-v2 wave came from `Post V2 User FeedBack, Features, Updates, Fixes.md`). Waves are gone as a concept (2026-08-05); ordering lives in the NEXT SESSION banner. **Known unticketed follow-up:** #172's memo fix merged without a fresh eval subagent, so if the Completion vital or warmth tint misbehaves live, start at `src/sync/selfLoadTasks.ts`. **Dispatching new slices stays blocked** until the **monthly spend limit** (`claude.ai/settings/usage`) is raised — triple-green cannot be met without eval subagents.
>
> **Telegram bot's owner-only S16c live verify (`afk-pipeline-out/s16c-verify-checklist.md`) remains the one human gate.** Merge gate unchanged: **triple-green** = CI (**every job**, incl `pwa-e2e` — `docs/agents/afk-pipeline.md` no longer contradicts itself on this) + ponytail-review + FRESH eval subagent vs the ticket's numbered DoD. Standing local gate: run **`npm run build`** (tsc), not just `npm test` — vitest/esbuild skips type-check. `[UI]` work: feed `docs/DESIGN_LANGUAGE.md`, tokens-only, honor reduced-motion via `useReducedMotion()`, don't break `data-testid="tab-bar"`.

**— previous session —**

Last updated: 2026-07-31. **Post-v2 WAVE 12 COMPLETE — S58 · S60 · S61 all merged triple-green, dispatched three-wide in parallel worktrees.** Shipped: **S61** (#162 → `a562db9`, closes **#158**) `GitTransport._readFiles()` now walks folders recursively with a `.json` allowlist scoped to `agents/` (`MAX_DEPTH=4`), so `agents/<name>/status.json` + `Briefs/<date>.md` finally reach the live app — FleetStrip and the daily brief were shipped-but-dead in production until this landed · **S60** (#163 → `5a5e347`, closes **#164**/**#165**) Warmth tile → static background tint in `Aurora.tsx` via new pure `src/lib/warmthTint.ts` (alpha capped ≈0.10 by construction), Completion % + done/to-do takes the vacated first vital slot (`completionVital` in `vitalsData.ts`), plus new `src/sync/selfLoadTasks.ts` collapsing the duplicated provider self-loads into one memoized read · **S58** (#166 → `063da78`) new **Tasks** tab (`src/components/tasks/TasksView.tsx`), tabs 6→5 (Home·Tasks·Money·Career·Agents), Domains/Pulse re-parented as `Segmented` sub-nav segments, Home slimmed to the check-in surface. `docs/DESIGN_LANGUAGE.md` §4.2 + §5 updated by their owning PRs; the S58/S60 §5 collision was resolved by hand at rebase (S60's `.vitals` line + S58's `nav.tabs` line, both verified present). Live site verified serving the new bundle. Filed **#167**/**#168** from the S60 eval. **Board: 72 done, 2 ready (S59, S62), 0 progress. Open issues: 3 (#155→S62, #167, #168). Open PRs: 0.**

**Every one of the three needed a revision, and in each case a gate caught what the author had reported as fine** — the gates are earning their cost, so do not shortcut them:
- **S61**: the eval disproved the "no `VaultSync.list()` guard needed for `Briefs/`" justification. `Calendar/today.md` is a machine template (`- ${start}-${end} …`) so its shape genuinely can't match the checkbox regex; `Briefs/<date>.md` renders `- ${l}` from **raw LLM output** with the no-leading-dash rule only in a system prompt, never enforced in code. Fixed as a CLASS: `list()`'s per-folder denylist (`Habits`, `Mail`) became one structural allowlist — `if (!isDomain(folderName) && folderName !== 'Inbox') continue` — which excludes `Briefs`, `agents`, `Calendar` and every future folder by construction. Mutation-proven (blanking the line fails exactly 3 tests).
- **S60**: the eval REFUSED the "pre-existing flake" exemption, ran the full suite twice on branch and twice on master, and proved `cockpitShell` failed 2/2 on branch / 0/2 on master. Root cause was Aurora adding a *second* concurrent provider read on every `<App/>` mount — fixed at the cause (`selfLoadTasks`), not by raising a timeout.
- **S58**: ponytail-review mutation-tested both DoD-critical guards and found them structurally incapable of failing (the Home-fold assertion ran before `seedIfEmpty` populated anything; the no-duplicate assertion leaned on `AnimatePresence mode="wait"` never having two tabs in the DOM). CI independently went red on the same test's 107-row seed race. Fixed with file-level `?noseed` + direct `db.tasks.bulkAdd(FIXTURE_TASKS)`.

**— previous session —**

Last updated: 2026-07-23. **v2 Glass Cockpit COMPLETE — 69/69 cards (100%).** Shipped the entire remaining HomeView chain + 3 transport fixes this session, each triple-green (CI incl pwa-e2e + ponytail-review + FRESH eval-vs-DoD): **S34** Today card (#152: `src/components/home/TodayCard.tsx` + pure `src/lib/gapFit.ts`) · **S37** attention stack (#156: `AttentionCard` renders S36 `parseAttention`) · **S48** fleet strip (#159: `FleetStrip`, reuses S47 `healthOf`/`parseStatus` + AgentsView LED table) · **S50** daily-brief agent (#161: `agents/daily-brief/brief.mjs` Claude-compose 5-line morning brief + `src/vault/briefs.ts` + Home am-only surface). **HomeView chain S34→S37→S48→S50 fully closed** — the whole v2 backbone (S20–S57) is done. Three transport folder-gap fixes, all bugs the eval surfaced live: **#148** Habits/ (#153, + a `loadGit()` init-order fix for a pre-existing CI teardown race the new test exposed), **#151** Calendar/ (#157, no VaultSync guard needed — verified), **#154** Mail/ (#160, +VaultSync `list()` guard, mutation-proven). Closed #148/#151/#154; filed **#155** (perf-seam) + **#158** (systemic `_readFiles()` folder-gap, STILL OPEN — agents/ + Briefs/ live self-loads resolve empty until it lands). Every PR triple-green. **Board: 69 done, 0 backlog/ready/progress. Open issues: 2 (#155, #158). Open PRs: 0.**

**⚠️ Master is 2 ahead of origin, 0 behind — owner push pending.** All three feature PRs (#162/#163/#166) were merged to origin server-side via `gh pr merge --squash` and pulled back, so the code is already on origin; the local-ahead commits are **board/handoff docs only, no code**: `a034f4e` (board: S61 → done) and `93ea496` (board: wave 12 COMPLETE) — plus *this* handoff commit. behind == 0 → clean fast-forward, no rebase needed: `git push origin master` (verify `git rev-list --left-right --count master...origin/master` reads `3 0` first). Push stays owner-gated.

**~~Housekeeping owed — 6 stale worktrees~~ — RESOLVED in session 2 (2026-07-31)**, along with the six later ones; see the session-2 housekeeping line at the top. `git worktree remove --force <path>` + `git worktree prune` worked without the feared Windows path-length trouble.

> **NEXT SESSION — wave 13 is unblocked, dispatch two-wide.** `/lifeos-boot` → `state.mjs` → sitrep → dispatch. Both prereqs merged this session, so `TabBar.tsx` and `vault/transport.ts` are both free; the two cards have **disjoint write-sets** (`state.mjs` reports zero hotspot conflicts) and MAY run in parallel worktrees:
> - **S59** (`docs/slices/slice-S59-responsive-tab-bar.md`, owner feedback #2, unblocked by S58) — tab bar must fit every screen. **Now confirmed live**: `TabBar.tsx` is `w-max mx-auto` with no wrap and no horizontal scroll, so on a narrow viewport the pill overflows centered and clips at both ends. Fluid metrics, same labels at 320px, keep `data-testid="tab-bar"`. Sole `TabBar.tsx` toucher.
> - **S62** (`docs/slices/slice-S62-transport-config-seam.md`, closes **#155**, unblocked by S61) — move the `GitTransport` config-check before the dynamic import, drop the per-card self-load short-circuits. Sole `vault/transport.ts` toucher. **Fold in while you're there:** `src/components/home/TodayCard.tsx` (~L27-30) still carries a stale comment describing the old flat `.md`-only folder list — S61 deliberately left it as out-of-write-set.
> - **Then the two new issues from the S60 eval** (both in `src/sync/selfLoadTasks.ts`, so do them together, and they touch `App.tsx` only if you also consolidate the third provider): **#167** (bug) `inFlight ??= provider.list()` caches the PROMISE, so a first-load rejection is cached forever and nothing retries — one transient IndexedDB error degrades the Completion vital and the warmth tint for the whole session · **#168** the same memo has no test reset hook; inert today only because every test injects `tasks`, but the first test that exercises the real load path will silently inherit a prior test's state.
> - **Design doc moves with the product.** `docs/DESIGN_LANGUAGE.md` is LOCKED, but owner feedback overrides it: a slice that changes a documented surface edits its own § in its own PR. The eval checks conformance — a stale doc fails a correct PR. §5's `nav.tabs` line and `.vitals` line are now current as of wave 12; don't re-edit them unless the product moves again.
>
> Beyond those: run the live product as real usage → file issues; the v1 user-testing checklist (deleted with `lifeos-hub.html`; `git show 7a06f62:lifeos-hub.html`) still stands. **Telegram bot's owner-only S16c live verify (`afk-pipeline-out/s16c-verify-checklist.md`) remains the one human gate.** Merge gate unchanged: **triple-green** = CI (incl **pwa-e2e**) + ponytail-review + FRESH eval subagent vs the ticket's numbered DoD. Standing local gate: run **`npm run build`** (tsc), not just `npm test` — vitest/esbuild skips type-check. `[UI]` work: feed `docs/DESIGN_LANGUAGE.md`, tokens-only, honor reduced-motion via `useReducedMotion()`, don't break `data-testid="tab-bar"`.

**Session lessons (2026-07-31 session 2, S59/S62 + #172 — wave 13, two-wide parallel) — add to the standing list:**

- **A test that models a property is not a test that measures it.** S59's guard parsed `clamp()` numbers out of `className` and ran a hand-rolled box model with constants transcribed from `App.tsx`. It was mutation-sensitive (breaking the 320px fit turned it red), so it wasn't vacuous — but it would drift silently the day the shell gutter changed, and the DoD had explicitly asked for computed layout. When the honest check needs a real browser, the write-set is a process constraint, not a technical one: widen it and add the ~12-line Playwright case rather than substituting a weaker guard.
- **Deleting a guard relocates its cost — measure the path you just opened up.** S62 removed per-card config short-circuits so the seam could own the check, which was correct, and thereby routed three more mounts into `getVaultPat()`'s blocking `window.prompt`. The DoD items all passed; the regression was on the exact path the slice existed to optimize. When a change moves a check from N call sites into one shared function, enumerate what *else* those call sites were incidentally skipping.
- **`jsdom` prompt/alert warnings are a real signal, not noise.** `Not implemented: Window's prompt() method` counts (10 → 25 on one file) were how the S62 regression got quantified. Grep them before/after any change that touches a credential or config path.
- **Worktrees share the local `master` ref.** An agent branching off local `master` inherited 6 unpushed docs commits and opened a PR with an 8958-line diff; it had to delete the remote branch (auto-closing #171) and cherry-pick onto `origin/master`. Put **"branch from `origin/master`, never local `master`"** in every dispatch prompt while local master is ahead — and push docs commits promptly so the window never opens.
- **A local e2e red under parallel dispatch is usually infra.** Both wave-13 authors called their e2e failures flake; both were green on the CI runner. The cause was `webServer.timeout` covering a full build (now 180s). Build, start `preview` yourself, then run Playwright — or just trust CI.
- **Subagent gates can die on account limits, and that is a hard stop.** #172's eval terminated on a monthly spend-limit error before doing any work. An agent should not merge on 2 of 3 and should not quietly promote its own checks to "the third green" — record them, name who ran them, and let the owner decide. (Here the owner merged, which is the right way for that call to get made.)
- **`git merge-base --is-ancestor <tip> origin/master` is the WRONG safety check in this repo — it says "unmerged" for everything.** Squash-merging rewrites branch content into a new commit, so a branch tip is never an ancestor of master. At worktree cleanup it reported all 11 tips as unmerged; trusting it means never deleting anything, inverting it means deleting on no evidence. The authoritative check is `gh pr view <n> --json headRefOid` — the tip must equal the recorded head of a **MERGED** PR. Diffing branch-touched files against master is also misleading: later slices edit the same files, so a non-zero residual proves nothing either way.
- **A `window.prompt` in a load path is a hard blocker for browser automation, not just a UX wart.** `getVaultPat()` prompts whenever `localStorage.lifeos_vault_pat` is empty, and the hosted build is vault-configured, so the very first vault read on a fresh browser profile raises a modal — which blocks every subsequent event the Chrome extension tries to send. Seed the token from a same-origin non-app URL first (`/LifeOS/manifest.webmanifest`), then navigate to the app. The proper fix is the long-deferred settings input noted under "Known follow-ups"; until then this is a live-testing setup step, not an optional nicety.
- **Delete-scope discipline: verify clean, verify merged, then delete only what was asked.** Cleanup found 33 orphan `worktree-agent-*` branches (harness residue, all zero commits ahead of master) and 3 empty orphan directories alongside the 12 worktrees — in scope, so they went. The 23 older merged feature branches (`afk/*`, `slice/*`, …) were NOT part of "clean up the worktrees" and were left, with a note offering. Check `status --porcelain` per worktree before any removal; all 12 were clean, which is what made the delete safe.

**Session lessons (2026-07-31, S58/S60/S61 — wave 12, three-wide parallel) — add to the standing list:**

- **A "documented pre-existing flake" claim is a hypothesis, not an exemption — make the gate run branch-vs-master, twice each, before accepting it.** S60's author cited the real `cockpitShell`/`syncProvider` fingerprint in `docs/agents/afk-pipeline.md` and claimed a `git stash` repro on master. The fresh eval measured properly: 2/2 fail on branch, 0/2 on master. The diff had added a second concurrent provider read. Inverse of the usual local-red/CI-green lesson — **CI was GREEN on that commit** because the runner is fast enough to absorb the extra read, so a green CI is NOT sufficient evidence that a new self-load is free.
- **A guard test that cannot fail is worse than no test — it certifies the guarantee it never checks.** Both of S58's DoD-critical assertions passed under a clean production revert: one asserted absence *before* the seeder populated anything, the other leaned on `AnimatePresence mode="wait"` never having two tabs mounted at once, so cross-tab duplication was structurally unobservable. Require the author to *watch the test go red*, and have the reviewer reproduce the mutation independently.
- **When a per-item denylist reaches its third entry, it IS the bug.** `VaultSync.list()` had grown `Habits` (#148) then `Mail` (#154) skips and was about to gain `Briefs`; the eval's suggested fix was that third string. Flipping to a structural allowlist (`!isDomain(folderName) && folderName !== 'Inbox'`) closed `Briefs`, `agents`, `Calendar` and every future folder at once. Same shape as the `_readFiles()` class fix in the same slice. **Verify an allowlist can't fail CLOSED** — grep every writer (here: `resolveFilePath`, the bot's own `services/bot/vaultPath.ts`, ADR-0010) before trusting it, since silently dropping real tasks is worse than the phantom-task bug it fixes.
- **Dispatch-harness lesson: subagents self-deadlock by inventing a background notification to wait on.** Six times this session — "waiting for the test run", "waiting for CI", "monitoring node process count". Nothing notifies a subagent. Put it in the dispatch prompt: *one blocking foreground `Bash` call, no `run_in_background`, no `&`, nothing will notify you.* Then inspect the worktree yourself and `SendMessage` a bounded finish — never cold re-dispatch, which discards intact work (all six had committed or uncommitted work worth keeping).
- **Resume rules by death cause:** API/network death (`ENOTFOUND`) → `SendMessage` resumes fine, transcript intact. **User-killed → the harness REFUSES to resume** ("treat its work as cancelled"); the worktree files survive on disk, so either finish it yourself or spawn a fresh agent pointed at that existing worktree with an explicit *finish-and-verify, do not restart* brief.
- **Orphaned `node`/`vitest` processes from sibling worktrees poison test timings.** One agent left ~20 running; another observed unrelated tests failing under the contention. Kill strays before measuring, and say so when reporting — a flake claim measured under unknown load is not evidence.
- **Two slices editing adjacent lines of the same doc section WILL conflict even when their write-sets are "disjoint".** S58 (§5 `nav.tabs`) and S60 (§5 `.vitals`) sat 3 lines apart; the second to merge conflicted. Resolve by hand and verify BOTH edits survive — a clean auto-merge here would have silently reverted one.

**Session lessons (2026-07-23, S34/S37/S48/S50 + #148/#151/#154 — v2 finale, 3 disjoint pairs + 1 solo) — add to the standing list:**

- **`GitTransport._readFiles()` hardcodes a flat `.md`-only folder allowlist — it silently drops any consumer folder not in the list.** 5 gaps found this session (Habits #148, Calendar #151, Mail #154, agents/ + Briefs/ #158). One-word-per-folder patching is the wrong fix past the second; #158 tracks the systemic root (recurse + non-md, or per-consumer allowlist). Every card self-loading a NEW vault folder is a live no-op until its folder is in the loop — the fixture DoD never catches it, the fresh eval does.
- **Surfacing a new folder can pollute `VaultSync.list()` — diff its line-format against `parseTaskLine`'s checkbox regex `/^- \[([ xX])\]…/` BEFORE adding it.** Mail lines are `- [ ]`-shaped → needed `if (folderName === 'Mail') continue` in `list()` (mutation-proven); Calendar lines (`- HH:MM …`) never match → no guard. Add the guard + a red-pre-fix test, or state precisely why none is needed.
- **`new LightningFS()` before the config-check orphans a `navigator`-touching init promise → post-teardown unhandled rejection, local-green/CI-red.** #148's new test exposed a PRE-EXISTING race (bare `<HabitsCard/>` self-load in HomeView tests): all tests pass but vitest exits 1 on the unhandled rejection. Fix: validate `VITE_VAULT_REPO_URL` before constructing the FS backend. Any GitTransport-path test MUST mock lightning-fs (never the real backend). Fingerprint: `ReferenceError: navigator is not defined … DefaultBackend.init`.
- **An API-path function behind an injection seam needs its OWN direct test.** S50's `callClaudeForBrief` (real HTTP + response parse) passed eval SHIP because every test injected a mock `callClaude` and bypassed it — a header/model-id/body/parse regression would fail nothing. Added 10 mocked-`fetchImpl` tests (request shape + non-2xx throws + non-JSON→null). Verify the model id against the `claude-api` skill, never memory (`claude-sonnet-5` current).
- Runs orchestrated manually (implement subagent → ponytail-review + fresh eval concurrent on the frozen diff → orchestrator merge), NOT via afk-pipeline P5/P6 — no `afk-pipeline-out/` run-manifest this session.

**Session lessons (2026-07-22 session 2, S41/S55/S45/S53/S54/S32 — 3 disjoint pairs) — add to the standing list:**

- **Local `npm test` does NOT type-check — CI `build-test` (`npm run build` = tsc) does.** S32 used `Array.prototype.at()`; vitest (esbuild transpile, no lib check) passed 984/984 locally AND in the fresh eval subagent, but CI `build-test` failed `TS2550: Property 'at' does not exist on type 'string[]' … change lib to es2022`. The local gate must include `npm run build` (or `tsc --noEmit`) or lib-gated APIs slip to CI. On the fix, grep the whole diff for the same API (only 1 occurrence here) — root-cause, not just the failing line. This is now a standing pre-push gate in the NEXT-SESSION banner.
- **Eval-flagged LIVE-path gap → file the issue, do NOT fix in the slice.** S32's DoD is all fixture/fake-transport tested (green), but the eval found `GitTransport._readFiles()` only walks `[...DOMAINS,'Inbox']` — never `Habits/` — so live taps overwrite log history. Out of S32's write-set (transport.ts is its own hotspot). Filed **#148** (repro + one-line fix). Same discipline as #136/S51: a passing merge gate doesn't mean the eval's product-gap findings are noise.
- **Raw hex that reproduces a DESIGN_LANGUAGE §-literal with same-file precedent is NOT a design-gate violation.** S53 (`#c4b5fd` §4.8 metric accent) + S54 (`#a5b4fc`/`#fca5a5` §4.4 action buttons) both used Tailwind arbitrary hex where no token exists; the eval verified each is the verbatim spec literal already used by sibling call-sites (AgentsView infra badge, Chip.tsx p3). Ship it; if a token gets minted later, migrate the cluster together. The gate exists to catch *invented* one-off colors, not spec literals.
- Runs were orchestrated manually (implement subagent → inline review → fresh eval → orchestrator merge), NOT via afk-pipeline P5/P6 — so no `afk-pipeline-out/` run-manifest or `LESSONS.md` line this session. If a future session runs the real pipeline, point here to that ledger instead of inlining.

**Session lessons (2026-07-22, S44/S52/S49/S56 — orchestration) — add to the standing list:**

- **A watchdog-killed subagent is RESUMED, not re-dispatched.** S56 stalled (600s no stream progress) mid-run with real uncommitted work in its worktree (`rotate.mjs`, tests, workflow) but no commit. `SendMessage` to its agent id continued it from its own transcript + worktree — it finished, committed, reported clean. A cold re-`Agent` would have thrown away the partial work and re-derived context. Send a **bounded** finish instruction (run tests → commit → report; "kill hanging commands, don't wait"), and inspect the worktree first (`git -C <wt> status/log`) so you know what state it left.
- **Push order for parallel-worktree PRs: master first, then rebase each branch `--onto master <old-base>`.** Push+rebase `master` onto origin, push it, THEN for each feature branch `git rebase --onto master <fork-point>` so the PR diff is only the feature commit. Skipping this makes the (rewritten) docs/board commits show as duplicates in every PR. `--onto` cleanly drops commits already upstream ("patch contents already upstream").
- **Windows `core.autocrlf=true` silently drifts LF fixtures to CRLF on every fresh worktree checkout** — it bit `supervisor.test.ts`'s byte-exact CRLF guard in 3 separate worktrees before being fixed at the root: `.gitattributes` → `<fixture-dir>/** text eol=lf`, then refresh the working tree (`rm` the files + `git checkout -- <dir>`). Fix it once at the repo root, never per-worktree. This is the CI-green/local-red class of failure — don't dismiss a local-only fail as "just my machine"; pin the bytes.
- Per-run afk-pipeline lessons were NOT generated this session — the dispatched agents stopped at commit-on-branch (P6 retro/run-manifest not run), and the orchestrator did the verify+merge. If future runs re-enable P6, point to `afk-pipeline-out/LESSONS.md` here instead of inlining.

*(The 2026-07-21 and 2026-07-20/#120 dated blocks were distilled into the standing `## Lessons / gotchas` list this close and removed from the head — mutation-test-guards, root-cause-across-siblings, re-dispatch-dead-reviewers, amend-don't-delete-flake-fingerprint, and don't-manufacture-load are now standing. The 2026-07-21 block's fetch-before-ahead/behind + scripted-close lessons were already baked into the `/lifeos-close` skill. The 2026-07-18 block was distilled a prior close.)*

### Wave 3 recap (this session, 2026-07-15)

8-wide fan-out, all merged triple-green:

| Slice | PR | What landed |
|---|---|---|
| S36 Mail/attention contract | #112 | `src/vault/mail.ts` — 5-label attention parser + provenance fields |
| S33 Calendar contract | #113 | `src/vault/calendar.ts` — events + `freeGaps` (overlap-merge, staleness date) |
| S30 Habits contract | #115 | `src/vault/habits.ts` — log parse/serialize roundtrip, `weekGrid`, `streak` |
| S43 Career contracts | #114 | `src/vault/career.ts` — pipeline stages + course progress |
| S39 Finance contracts | #117 | `src/vault/finance.ts` — networth/portfolio/burn/bills + `formatINR` |
| S47 Agent-run contract | #118 | `agents/lib/runLog.mjs` + `src/vault/agentStatus.ts` — `healthOf` thresholds |
| S25 Cockpit header | #119 | `src/components/cockpit/Header.tsx` — greeting + time seg + note (CSS reduced-motion) |
| S26 Vitals row | #116 | `src/components/cockpit/VitalsRow.tsx` — 5 tiles, live warmth, 4 stubs |

**Concurrency ceiling is 2 (owner-set 2026-07-22, was 4) and fan-out is now mostly spent** — dispatch at most two disjoint slices at a time; remaining depth is the serial chains in the banner above. `src/App.tsx` (S24, done) and `HomeView.tsx` are the irreducible hotspots; never batch a hotspot-sharing slice (`afk-pipeline-out/LESSONS.md`). Full slice map: `docs/slices/README.md`.

## v2 vision + design lock (this session, 2026-07-08 pm) 🎨
- **LifeOS v2 = life cockpit, not task tracker.** Grill session locked scope: time-aware check-in cockpit (morning brief / midday check / evening review), Today's Mission (1–3 balance-brain picks, why + done_when always visible), unified Attention stack (client email, job replies, bills, agent failures — Gmail-fed), Life Vitals row, calendar blocks + gap hints, habits (each habit FEEDS a domain's warmth), money (net worth/burn/portfolio/bills; Zerodha/Groww/CSV first), career tab (job pipeline kanban + course progress), agent fleet health board incl. a **supervisor** agent (weekly log audit, accuracy metrics, prompt patches gated on owner approval — confirm-destructive spirit extends to agent self-modification). Full detail: `memory/lifeos-v2-vision-2026-07-08.md`.
- **Design LOCKED: Glass Cockpit.** Three mockups built (`docs/mockups/cockpit-glass.html` ← chosen, `cockpit-terminal.html`, `cockpit-edition.html` — the other two kept for reference only). The contract extracted from the winner lives in **`docs/DESIGN_LANGUAGE.md`** — tokens, component specs, layout/IA (6 tabs: Home/Money/Career/Agents/Domains/Pulse), time-of-day system, motion/a11y, Do/Don'ts. Load it into every future coding session; deviations need a written one-line reason in the PR.
- Nothing shipped to `src/` this session — docs + mockups only.

## v2 progress (Glass Cockpit — slices S20–S57)

**✅ COMPLETE 2026-07-23 — all 38 v2 slices (S20–S57) shipped, every phase/wave done.** The detail below is the ARCHIVAL Wave-0 kickoff snapshot, kept for provenance; live progress is the board (69/69) + the recap at the top of this file.

**Wave 0 done: S20 — design tokens (#103 → PR #104, merged `dc47069`).** `docs/DESIGN_LANGUAGE.md` §2 is now code: `src/styles/tokens.css` (all 17 §2.1 colors, §2.2 font stack, §2.3 radius/blur vars, `color-scheme: dark`), the §2.4 Tailwind mapping, and `src/index.css` on `--bg`/`--txt`. 379/379 tests green. Two things the next session must know:

- **The app still LOOKS like v1.** `body` sits on `--bg` now, but `src/App.tsx:79` paints a v1 light time-of-day gradient inline over it at runtime. `App.tsx` is **S24's exclusive hotspot** (out of every other slice's write-set), so the dark ground only becomes visible once **S22 (aurora) + S24 (shell)** land. This is expected — not a regression, don't "fix" it in a S21/S22/S23 PR.
- **The Tailwind extend is MERGED, not replaced.** v2's §2.4 keys sit *alongside* the v1 `apple-*` / `rounded-ios` / `shadow-glass-*` keys, because every shipped v1 component still uses them; replacing outright breaks the app. The v1 keys retire per-view as S21+ restyles each surface. A test in `src/test/tokens.test.tsx` pins them compiling until then.
- **Known cruft, deliberately not touched:** `tailwind.config.js` still carries a v1 `spring: cubic-bezier(0.34, 1.56, 0.64, 1)` bounce easing that the v2 motion contract (§7) has no room for. Out of S20's write-set. Kill it in a v1-teardown slice, or let it die as the v1 keys retire.

**The eval gate earns its keep on `[UI]` slices** — on S20 it caught `rgba` spacing drift between `tokens.css` and `tailwind.config.js` that both CI *and* review passed over. "Byte-exact to the contract" needs a reader that diffs characters. Keep dispatching it fresh (no shared context with the implementer).

## What LifeOS is
A personal, Apple-feel life tracker for a single user (the repo owner), built local-first as an installable PWA that runs offline on Windows and Android, with an Obsidian markdown vault as the real source of truth and three faces: **PWA dashboard, Telegram bot, Obsidian itself.** Seed data in `seed_tasks_detailed.json` (107 tasks) captures long-term intent. Read `CONTEXT.md` for the glossary, `docs/archive/V1_ARCHIVE.md` for the archived v1 slice backbone + product vision (v2 backbone: `docs/LIFEOS_V2_ROADMAP.md`, slices S20–S57), `memory/lifeos-vision-2026-06-22.md` for full design rationale.

## Current state (on `master`, tip `e6c8a62`)
**Slices S1–S19 ALL complete + the PWA is DEPLOYED LIVE.** Working installable offline PWA (nav shell, glass look, smart capture, Pulse, NOW balance-brain), a live Obsidian-vault backend behind `VITE_VAULT=1`, and a full Telegram bot (`services/bot/`): text→create, confirm-gated update/delete, photo→vision batch-confirm, voice→transcription. The hosted PWA now clones a real private vault repo in-browser through a self-hosted CORS proxy and loads real tasks — verified on desktop and mobile.

- **MVP (Groups A–C, S2–S13):** Task model (`done_when`/`priority`/`project`/`domain`/`completed_at`), NOW balance-brain (per-domain cap + cold-domain rescue), tap-the-dot complete + undo, derived domain warmth, glass/time-of-day skin, smart-capture `+` sheet, Pulse trends. All pure seams (`rankNow`, `computeWarmth`, `parseCapture`, pulse metrics).
- **Vault sync (Group D, S14–S15):** git-as-transport read + write; vault is the real truth. `VaultSync` provider swapped at the seam when `VITE_VAULT=1`. Single-line splice via in-memory source-map, FIFO write-queue, local-authoritative commit + best-effort push, wipe-reclone data-loss guard.
- **Bot (Group E, S16 — MERGED, live):**
  - **S16a (#64→PR #67):** durable `id::` vault identity. `parseTaskLine` reads optional `id::`; `serializeTaskLine` always emits it (after title). Legacy lines stamped lazily on next write — **no schema change, no bulk migration.** ADR-0011 §3.
  - **S16b (#65→PR #68):** bot core in `services/bot/` — long-poll worker, owner-chat-id guard, `claude-sonnet-5` structured-output NLU (create-only), self-registering intent router (`intents/` + `registry`), over a fake transport. Adds a `bot-test` CI job; `vite.config.ts` excludes `services/**` from the PWA vitest.
  - **S16c (#66→PR #69, HITL):** real Node `isomorphic-git` transport (commit-local → best-effort push, wipe-guard ported from S15b) + live Telegram wiring. Bot's own `BOT_VAULT_PAT`. **⚠️ Merged CI-green but the 5-case LIVE verify was NOT run** — see "Outstanding HITL" below.
  - **S19a (#71→PR #81):** photo ingest — `telegramClient.downloadPhoto` + `visionExtract.ts` (`claude-sonnet-5` vision, `{tasks:[…]}` structured output, cap 20). Router-free, additive. 54/54 bot tests.
- **Bot (Group E finale — MERGED 2026-07-05):**
  - **S19b (#72→PR #83):** photo batch-confirm — `photoConfirm.ts` (per-chat pending Map, 10-min TTL) + photo/confirm branches in `router.ts`; `all`/`none`/subset reply picks which extracted tasks get created via the unmodified `handleCreate`. 71 bot tests.
  - **S17 (#75→PR #85):** confirm-destructive update/delete — `taskMatch.ts` (fuzzy target match), `confirm/store.ts` (2-min TTL pending Map), `confirm/gate.ts` (y/n/disambiguation state machine, sole vault-writer), self-registering `intents/update.ts`+`delete.ts`. Router gate runs before NLU. ADR-0013. 131 bot tests.
  - **S18 (#78→PR #87):** voice notes — `transcription.ts` (Groq Whisper `whisper-large-v3-turbo`, never-throws, confidence = non-empty text AND mean `no_speech_prob` ≤ 0.5) + `downloadVoiceFile` + router voice branch; confident transcript flows through the *unmodified* `classifyAndExtract`→dispatch path, reply echoes `heard: '…' → …`. New env `GROQ_API_KEY`. ADR-0014. 141 bot tests. Zero diff in `intents/**` + `nlu.ts`.

Repo: `Deepak-Lakshmipathi/LifeOS` (public), default branch `master`. Ship via branch + PR — direct push to `master` is gated.

**Open issues:** none. All slice + PRD-parent issues (#70/#74/#77) closed. **Open PRs:** none. **Kanban:** all 30 cards `done` (the board tracks slices only; deployment infra below is post-backbone and lives in this handoff, not on the board).

## Deployment (live) 🚀 — added this session
The PWA face is hosted, installable, and wired to a real vault. Everything is single-user and keeps the vault PAT off any public bundle.

- **Live URL:** https://deepak-lakshmipathi.github.io/LifeOS/ — GitHub Pages, source = **GitHub Actions**. Auto-deploys on push to `master` via `.github/workflows/deploy-pages.yml` (build `VITE_VAULT=1`, `VITE_BASE=/LifeOS/`, **no PAT in the build env**). Pages serves under the `/LifeOS/` subpath — Vite `base` + base-relative manifest `start_url`/`scope`/icons (see `vite.config.ts`).
- **Vault repo:** `Deepak-Lakshmipathi/LiveOS-VaultRepo` (PRIVATE, default branch `main` — note the name is "**Live**OS", not "Life"). Seeded with all 107 tasks from `seed_tasks_detailed.json` (18 `<Domain>/<Project>.md` files, `serialize.ts` format). Distinct from the app repo. The bot writes here too (its own `BOT_VAULT_PAT`).
- **Runtime PAT (never baked):** `src/vault/pat.ts` reads the token from `localStorage`, prompting once on first load. The `import.meta.env.VITE_VAULT_PAT` fallback is guarded by `import.meta.env.DEV` so a production build dead-code-strips it. **Never set `VITE_VAULT_PAT` in the Pages build env** or it inlines into the public JS. Recovery UI: the error panel's "Re-enter token" calls `clearVaultPat()` + reload.
- **Self-hosted CORS proxy:** `cors-proxy/` — a Cloudflare Worker (`worker.js`, deployed via `wrangler`), live at `https://lifeos-git-proxy.ldeepak-kumar550v.workers.dev`. Replaces the public `cors.isomorphic-git.org` so the PAT + git traffic transit only the owner's Cloudflare account. Locked to the LifeOS origins + github.com git endpoints (not an open relay); maps isomorphic-git's `x-authorization` → `authorization`. The Pages build reads the URL from **repo variable** `VAULT_CORS_PROXY` (`gh variable set …`), so it isn't committed. Node self-check: `node cors-proxy/worker.test.mjs`.
- **Load fixes shipped this session (why it now works in-browser):**
  - `GitTransport.readFiles` dedupes concurrent callers behind one in-flight clone/pull — App fires two `list()` on mount (`seedIfEmpty` + `useTasks`) and two parallel clones into the same lightning-fs dir raced and rejected (PR #91).
  - `useTasks` initial load now `.catch`es and surfaces `error` (App shows an error panel + Retry/Re-enter) instead of a forever-spinner (PR #91).
  - `src/main.tsx` shims `Buffer` + `process` (from the already-present `buffer` dep) before any vault code loads — isomorphic-git references those Node globals and the browser threw `Buffer is not defined` (PR #92).
- **Session PRs:** #89 (Pages deploy + runtime PAT), #90 (self-hosted CORS proxy), #91 (load-hang fixes), #92 (browser Buffer/process shim). All merged.
- **To redeploy / operate:** merge to `master` (auto-deploys). To rotate the proxy: `cd cors-proxy && npx wrangler deploy`. To change the proxy URL: `gh variable set VAULT_CORS_PROXY --body <url>` then re-run the deploy workflow. Local dev vault mode still uses `.env` (gitignored; `.env.example` is the template).

## Outstanding HITL — owner-only, cannot be automated ⛔

**S66 DoD #12 — vault FS lock check (owed since 2026-08-05, still not run).** On the deployed Pages build with DevTools open, **hard-reload first** (Ctrl+Shift+R, or Application → Service Workers → "Update on reload" — it is a PWA and a soft refresh serves the cached old build), then hard-reload across **two full mount cycles including one where the remote 401s**, and confirm **zero** `Lock broken by another request` (pre-fix: 7 per cold load + 1 per habit tap). Also confirm TodayCard/AttentionCard render real content rather than a confident empty state — a ghost file used to render as "no events today" with no error at all. Paste filtered console output into PR #192. **Structurally not CI-verifiable:** jsdom has no `navigator.locks`, so `DefaultBackend.init` selects the IDB `Mutex` rather than `Mutex2` and the steal mechanism is absent from the test environment entirely. A test that mocks `navigator.locks` asserts its own mock (the #120 failure mode) and must be rejected in review. Owner-sanctioned as HITL at dispatch (S66 owner decision 6), same category as ADR-0010 flag (D).

**#186 + #183 live verify (NEW, owed now — same visit as S66 DoD #12 above).** Both are deployed. Confirm by hand, because neither is fully CI-provable: **(1)** Home shows **no** `+ New task`; **(2)** the Tasks tab shows it **above** the sub-nav and it stays put when you switch to Domains and Pulse; **(3)** completing a task via the mission dot moves the DAY REVIEW **Completion** counter **without a reload** — that is #186's entire point, and it is the thing that was broken. The Aurora warmth tint only shifts for **domain-tagged** tasks (`computeWarmth` is domain-based), so a domain-less task leaving the tint unchanged is correct, not a bug — that asymmetry is exactly why #195 exists.

**S16c live verify — Telegram bot (standing, still not run).**
S16c shipped CI-green but the git-network + live-Telegram path is not CI-verifiable (no remote/token/key in CI). The 5-case checklist at `afk-pipeline-out/s16c-verify-checklist.md` has **not** been run. Before trusting the bot in production, the owner must: set up `services/bot/.env` (bot-scoped `BOT_VAULT_PAT`, Telegram token via @BotFather, owner chat-id, `ANTHROPIC_API_KEY`), `cd services/bot && npm install && npm start`, then verify: (1) owner text → real commit (author `LifeOS Bot`, non-empty `id::`) → pushed → shows on PWA → `✓ added` reply; (2) offline commit survives + pushes on reconnect; (3) non-owner ignored; (4) ambiguous domain → Inbox; (5) non-create intent → "not yet supported". **An agent cannot do this.**

## Next task — v2 EXECUTION 🚀
v2 is fully prepped: design LOCKED (`docs/DESIGN_LANGUAGE.md`), backbone sliced + numbered (`docs/LIFEOS_V2_ROADMAP.md`, S20–S57), afk-pipeline v2 config seeded (`docs/agents/afk-pipeline.md`, PR #96), v1 archived (`docs/archive/V1_ARCHIVE.md`).

**Tickets are CUT (2026-07-14):** all 38 v2 slices have dispatch-ready tickets in `docs/slices/slice-S20…S57` (context, write-set, subtasks, numbered DoD, tests) Dep graph + hotspot chains were kanban `blockedBy` edges until 2026-08-05; they are now `Blocked by: #N` lines in issue bodies, and hotspot batching is a by-hand write-set check. Rules: `docs/slices/README.md`. Merge gate is now **triple-green** — CI + review + an eval subagent that checks the PR diff against the ticket's DoD (process in `docs/agents/afk-pipeline.md` "Eval gate").

Order of operations:
1. **Dispatch Phase 0** (S20 tokens → S21 primitives → S22 aurora → S23 timeOfDay) via afk-pipeline — S21/S22/S23 all dep on S20, so S20 lands first, then the other three can run parallel (disjoint write-sets).
2. Then Phase 1 (shell: S24 pill-tabs, S25 header, S26 vitals row) → Phase 2 (Home from existing data) → fan out Phases 3–7 in parallel.
3. **In parallel, owner runs the S16c bot live-verify** (see Outstanding HITL below) — the last v1 production-trust gate. An agent cannot do it. Prep is done (`.env` autoload, checklist updated for `GROQ_API_KEY`).
4. v1 user-testing checklist (deleted with `lifeos-hub.html`; `git show 7a06f62:lifeos-hub.html`) still stands — walk it as real usage alongside v2 work; feedback → issues.

Known follow-ups (non-blocking): the Pages deploy workflow's `actions/*@v4` run on a deprecated Node 20 (bump when convenient); the runtime PAT uses a `window.prompt` (fine for single-user; a small settings input is the obvious upgrade).

## Outstanding board state
**GitHub issues are the source of truth. Run `node .claude/skills/lifeos-boot/scripts/state.mjs` — do not maintain a count here.** As of 2026-08-05 (session 2): **2 open, both `status:ready`** (#194, #195) · **12 cold-storage** (closed, revivable) · 0 in-progress · 0 blocked · 0 needs-triage · **59 closed**.

**Historical, archival, not recomputed:** 79 slice-cards shipped across v1 + v2 + post-v2 + wave-14 (v1 S1–S19 = 30 cards + v2 S20–S57 = 38 slices + `bug-136` + post-v2 S58–S62 + S63/S64/S66 + #186/#183). v2 chains all complete: HomeView (S27→S28→S29→S32→S34→S37→S48→S50), VitalsRow (S26→S41→S45), AgentsView/Supervisor (S49→S53→S54, S55). #155 closed by S62; #158 by S61; #164/#165 by S60; #167/#168 by #172; **#173 by S63; #174 by S64; #176 by S66; #186 by PR #193; #183 by PR #196.** `state.mjs`'s `counts.done` is **closed issues only** (59) and will never match the 79 — most slices predate issue-per-slice tracking. Don't reconcile them.

**The wave-14 live-testing backlog is fully resolved or parked.** Waves are gone as a concept; the `S(N−110)` numbering below is archival. ~~S63/#173~~ ✅ · ~~S64/#174~~ ✅ · ~~S66/#176~~ ✅ · ~~S73/#183~~ ✅ · ~~#186~~ ✅. Everything else went to **cold storage** on 2026-08-05: #175 (one-line manifest deletion), #177 (staleness indicator), #178 (S68 — captured task never appears), #179+#180 (S70 — fabricated header progress / mode drives only the greeting), #181 (S71 — CaptureSheet stays mounted at opacity 0), #182 (S72 — 3s undo window after an unbounded wait), #184 (write amplification on page load — **re-measure before reviving**, S66 collapsed 7 concurrent `git.pull` to 1 and its numbers are pre-S66), and **S66's four deferrals: #188** cross-tab ownership (defers only while the non-stealing reset stands — anything reintroducing an unguarded wipe re-escalates it to a corruption vector), **#189** five cards each doing a full vault read, **#190** collapse the two `VaultSync` instances (**#186 already removed one of them**), **#191** `appendHabitHit` bypassing the FIFO.

**Currently open:** **#194** `pwa-e2e` offline-persistence flake (5 firings, stabilization owed since 2026-07-10, degrades a merge gate) · **#195** Aurora warmth-tint liveness coverage. Write-sets disjoint; both startable now.

## Architecture (decided — do not re-litigate)
- **Stack:** Vite + React + TS, Tailwind, Framer Motion, Dexie/IndexedDB (PWA); Node/TS long-poll worker (bot). ADR-0001.
- **Data access via a seam:** `src/sync/SyncProvider.ts`; impls `LocalOnly` (Dexie) and `VaultSync` (git vault). UI/components/hooks import only `SyncProvider` + `src/types`. Provider swapped in `App.tsx`. ADR-0002.
- **Mutation generic** (ADR-0004): `add(input)` + one `update(id, patch)`. New fields widen the patch; they don't add methods.
- **Not every field indexed** (ADR-0005): `project`/`domain`/`completed_at` are denormalized, no Dexie index, schema stays **v2**.
- **`id::` durable identity** (ADR-0011 §3): vault-format field only; no `Task`/Dexie change; lazy backfill.
- **Bot ↔ PWA meet only at the vault git repo**, never in-memory (ADR-0011). Bot has its own `BOT_VAULT_PAT`, runs as a long-poll worker (ADR-0011 §1). Intent router = self-registering handlers in `services/bot/intents/`, one file + one append-only import per intent (ADR-0011 §4) — BUT the message-ingest layer (`router.ts` `handleIncomingMessage`) is a shared hotspot the modality slices all touch.
- **Bot NLU ≠ PWA capture:** bot uses `claude-sonnet-5` structured output; the PWA `+` sheet uses the regex `parseCapture` — deliberately uncoupled (ADR-0011).
- **Testing:** CI gates `build-test` (Vitest, PWA), `bot-test` (Vitest, `services/bot`), `pwa-e2e` (Playwright install/offline/persistence). ADR-0003.

## Key files
```
src/…                       PWA (see prior handoff history / CONTEXT.md)
src/components/tasks/TasksView.tsx   Tasks tab shell (S58) — Segmented sub-nav (tasks|domains|pulse). #183: also the app's ONLY capture surface — `+ New task` + CaptureSheet, button ABOVE the sub-nav so it shows on all three segments. Otherwise pass-through props, no data logic
src/lib/warmthTint.ts       pure warmth→background-tint blend (S60); DOMAIN_HEX mirrors the 7 --d-* tokens, drift-guarded by src/test/tokens.test.tsx
src/hooks/useTasks.ts       HOTSPOT. S64: all four write paths optimistic, zero refresh() — commit()/tasksRef/localVersionRef/inFlightRef + the XOR owedRef latch. toggleDone's JSDoc synchronous-flip CONTRACT is load-bearing for S72
docs/adr/0015-shell-owns-no-presence-protocol.md   S63 — why App.tsx must never re-introduce AnimatePresence; names both implicit registration classes (exit, and layout/layoutId via MeasureLayout)
docs/adr/0016-single-owner-vault-fs.md   S66 — ONE process-wide owner of FS_NAME (getVaultTransport()); non-stealing in-place fs.init() reset. Read before adding any new vault reader: a 9th `new GitTransport()` silently re-opens #176. Amends ADR-0010 by link; that ADR's commitsAhead gate is untouched
src/test/shellNavigation.test.tsx  S63 Tests A+B — A mounts a panel that never completes its presence obligation (invariant, not journey-replay); B pins App.tsx free of AnimatePresence
src/test/useTasksOptimistic.test.tsx  S64 (a)-(f2) — (e4) is the convergence test that runs the latch to REST against a stateful provider; the others all stop at the first settle
src/test/completionLiveUpdate.test.tsx  #186 — drives the real <App/>: complete a task via the mission dot, assert the Completion tile's counts move in the same mount cycle. Asserts the `sub` line ("N done · M to do"), not the percent, because the percent goes through Vital's 900ms count-up. Reuse this harness for #195
(deleted 2026-08-05) src/sync/selfLoadTasks.ts   was a memoized provider.list() shared by Aurora + VitalsRow (S60). Deleted by #186 — it was a SECOND owner of the task list with no production invalidation path. `useTasks` in App.tsx is the only owner now; both components take `tasks` as a required prop. Do not reintroduce a self-load here
src/vault/parseVault.ts     parseTaskLine reads optional id:: (S16a)
src/vault/serialize.ts      serializeTaskLine always emits id:: (S16a)
services/bot/index.ts       long-poll worker entry; constructs real transport from config (S16b/c)
services/bot/router.ts      handleIncomingMessage — owner guard, then voice/photo/confirm branches, then NLU dispatch (was the modality hotspot; now settled — all four modalities landed)
services/bot/nlu.ts         claude-sonnet-5 structured-output intent+extract (create/update/delete); ClaudeClient + CLAUDE_MODEL exported here
services/bot/intents/        self-registering handlers: create.ts (S16b) + update.ts/delete.ts (S17) + registry.ts + index.ts (ADR-0011 §4)
services/bot/taskMatch.ts   fuzzy target-task match for update/delete (S17)
services/bot/confirm/        store.ts (2-min TTL pending Map) + gate.ts (y/n/disambiguation state machine, sole vault-writer for edits) (S17)
services/bot/photoConfirm.ts    per-chat pending photo batch (10-min TTL); all/none/subset picks (S19b)
services/bot/telegramClient.ts  getUpdates long-poll + sendMessage + downloadPhoto (S19a) + downloadVoiceFile (S18)
services/bot/visionExtract.ts   claude-sonnet-5 vision → {tasks:[…]} cap 20 (S19a)
services/bot/transcription.ts   Groq Whisper voice→text + confidence gate; never throws (S18)
services/bot/vaultTransport.ts  real Node isomorphic-git transport (S16c)
services/bot/config.ts      env: TELEGRAM_BOT_TOKEN, BOT_VAULT_PAT, BOT_VAULT_REPO_URL, ANTHROPIC_API_KEY, GROQ_API_KEY, OWNER_TELEGRAM_CHAT_ID
docs/adr/0011-…             bot transport / identity / router seam (S16)
docs/adr/0012-bot-photo-vision.md      S19 (photo)
docs/adr/0013-bot-confirm-destructive.md  S17 (confirm)
docs/adr/0014-bot-voice-transcription.md  S18 (voice; renumbered from a parallel-run 0012 collision)
docs/DESIGN_LANGUAGE.md     LOCKED v2 visual contract (Glass Cockpit) — read before ANY UI work
docs/LIFEOS_V2_ROADMAP.md   v2 slice backbone (one slice = one issue = one subagent)
docs/mockups/               cockpit-glass.html (chosen) + terminal/edition (reference only)
.claude/skills/lifeos-boot/scripts/state.mjs   THE board. Computes in-progress/ready/blocked/cold + git ahead-behind from GitHub issues. Labels ARE the state (2026-08-05); kanban.html + lifeos-hub.html + build-hub.mjs deleted, recover at `git show 7a06f62:<file>`
afk-pipeline-out/           LESSONS.md + s16c-verify-checklist.md (v1 deploy tables collapsed into docs/archive/V1_ARCHIVE.md)
src/main.tsx                shims Buffer + process (browser) before any vault code loads
src/vault/pat.ts            runtime PAT (localStorage prompt); DEV-guarded env fallback so it never bakes into a build
src/vault/transport.ts      GitTransport + getVaultTransport() — the ONE process-wide owner of FS_NAME (S66/#176; never construct GitTransport directly, ADR-0016). readFiles() dedupes via inflight AND hands each caller a defensive copy (VaultSync mutates entries in place). Reset is in-place fs.init(), never a 2nd LightningFS. _readFiles() walks [...DOMAINS,Inbox,Habits,Calendar,Mail,agents,Briefs] recursively; content-type guard drops non-string reads. loadGit validates url BEFORE the dynamic import (#153/#155). The commitsAhead>0 wipe refusal is ADR-0010 and is untouchable
cors-proxy/                 self-hosted Cloudflare Worker CORS proxy (worker.js + wrangler.toml + worker.test.mjs + README)
.github/workflows/deploy-pages.yml   build (no PAT) → deploy GitHub Pages on push to master
.env.example                PWA vault-mode config template (VITE_VAULT_REPO_URL/PAT/CORS_PROXY)
src/vault/supervisor.ts     supervisor report/proposal parsers + byte-preserving setProposalStatus (S52)
src/data/agentManifest.ts   7-agent fleet roster {name,purpose,infra,cadence} for the Agents tab (S49)
agents/lib/rotate.mjs       rotateJsonl (month-split/archive) + pruneNetworth; atomic+idempotent, zero-dep (S56)
.github/workflows/agent-rotate.yml   monthly cron pruning runs.jsonl + networth history, one commit (S56)
.gitattributes              pins src/vault/__fixtures__/** to eol=lf (Windows autocrlf CRLF guard)
src/lib/vitalsData.ts       pure VitalsRow selectors: netWorthVital/burnVital (S41) + pipelineVital (S45)
agents/supervisor/audit.mjs weekly fleet audit → S52 report + pending-only proposals; cap-N=15 mockable Claude sampling (S55)
.github/workflows/agent-supervisor.yml   cron Sun 06:00 IST supervisor run (S55)
src/components/agents/SupervisorCard.tsx  renders the weekly report on the Agents tab (S53)
src/vault/proposalWrite.ts  flipProposal — byte-surgical status flip via transport seam; no fs/git (S54)
src/components/agents/ProposalList.tsx    pending-proposal list + two-step approve/reject (S54)
src/vault/habitsWrite.ts    appendHabitHit — one S30-serialized hit line via transport (S32); NOTE live-read gap = bug #148
src/components/home/HabitsCard.tsx        §4.6 7-day grid + streaks + tap-today (S32; head of HomeView chain)
src/components/home/TodayCard.tsx         §4.5 calendar card — slots/tinted chips/gap hints (S34)
src/lib/gapFit.ts           pure gapFit(gaps,tasks) — top-ranked fitting task per gap (S34)
src/components/home/AttentionCard.tsx     §4.4 "Needs you" stack — rows/provenance/actions (S37)
src/components/home/FleetStrip.tsx        §4.7 fleet mini pills — LED per S47 healthOf, blink-on-fail (S48)
src/vault/briefs.ts         pure parseBrief(md) + latestBriefPath(today) (S50)
agents/daily-brief/brief.mjs  morning-brief agent — vault→context-pack→claude-sonnet-5 compose→Briefs/<date>.md; retry-once-fail-loud; own PAT (S50)
.github/workflows/agent-daily-brief.yml   cron 05:30 IST daily-brief run (S50)
```

## Run it
```
npm install && npm run dev            # PWA
npm test                              # PWA Vitest  (note: 'Seam isolation' test can TIMEOUT locally on a slow box — known flake, trust CI)
npm run build && npm run preview
npx playwright install chromium && npm run test:e2e
cd services/bot && npm install && npm test   # bot Vitest (141 tests)
cd services/bot && npm start          # run the live bot (needs .env: 6 secrets incl GROQ_API_KEY for voice)
node .claude/skills/lifeos-boot/scripts/state.mjs   # project state from GitHub issues
```

## Lessons / gotchas (carried + new this session)
- **Project state lives on GitHub, not in a file (2026-08-05).** `kanban.html`, `lifeos-hub.html` and `scripts/build-hub.mjs` were deleted — the owner didn't use the hub, and one board that can disagree with the issues is worse than none. An issue's **labels are its state**; `cold-storage` + closed means parked-not-abandoned. `state.mjs` computes the frontier and flags `labelDrift` (labels contradicting the dependency graph) — fix drift, don't ignore it. **There are no waves.** Batching two slices concurrently is now a by-hand write-set disjointness check. Recover the old board with `git show 7a06f62:kanban.html`.
- **The three router-hotspot slices (S17/S19b/S18) were shipped SERIALLY and it worked cleanly** — each its own worktree-isolated Sonnet implementer off fresh `origin/master`, dual-green → squash-merge → tiny `docs/<slice>-board-flip` PR → next slice. Every later slice rebased onto the prior router edit with zero conflicts. Confirms the serialize-don't-batch rule for shared-file slices.
- **Parallel git-writing subagents MUST use `isolation: "worktree"`.** This session ran S17/S18/S19 spec pipelines concurrently in ONE shared checkout — they raced on `HEAD` (one agent's commit landed on a sibling's branch). Both self-recovered (cherry-pick back, reset sibling to origin), no work lost, but it was luck. S19a was then dispatched with `isolation: "worktree"` — no race. Always isolate concurrent agents.
- **`services/bot/router.ts` is a modality hotspot.** S17/S18/S19b all extend `handleIncomingMessage`. The intent-router *registry* (ADR-0011 §4) was designed disjoint (one file per intent), but the ingest dispatch in `router.ts` is shared — so modality slices serialize. Confirmed by two independent pipelines flagging it.
- **A stale docs branch is a merge trap once code has landed.** The S16 docs branch was cut before the S16a/b/c code PRs; a direct merge would have reverted `services/bot` + `src/vault`. Fix: rebuild docs on a fresh branch off current master and cherry-pick only the doc artifacts (verify the staged diff is docs-only before commit). This is why PR #80 (Group E docs reconciliation) exists instead of merging the four pipeline branches directly.
- **Parallel pipelines pick colliding ADR numbers.** S18 and S19 both grabbed 0012. Renumber during central reconciliation (S18 → 0014) and fix the internal refs in the slice's deploy/prd/issue docs.
- **A subagent can hit the account session limit mid-run.** S16b's implementer died before committing; its work survived UNCOMMITTED in the (shared) worktree. Recovery: verify locally (run the suite), stage only the slice's files (exclude pre-existing untracked), commit/push/PR yourself, let CI gate. Always check `git status` + `gh pr list` before assuming a silent agent's work is lost.
- **HITL splits the gate:** CI green covers only what runs without a remote. Isolate the non-CI-verifiable part (S15b, S16c) into its own slice so only the minimum needs an owner hand-verify.
- **`pwa-e2e` occasionally flakes on an SW-timing race** (offline-persistence test, `emu-test` not visible). If the diff didn't touch `src/`/`e2e/`, re-run the job once (`gh run rerun <id> --failed`) before treating it as real.
- **Local `npm test` exit-1 ≠ CI red:** the `Seam isolation` static-analysis test can time out on a slow machine while CI is green. Trust CI as the gate.
- **The Obsidian vault (`LifeOS-Vault/`) is graphify OUTPUT, not source** — never hand-edit; exclude `LifeOS-Vault/`, `graphify-out/`, `.obsidian/` from re-graphifying.
- **isomorphic-git in the browser needs Node-global shims + concurrent-clone dedupe.** It threw `Buffer is not defined` on the hosted vault load (also reads `process.env`/`platform`); shim both in `src/main.tsx` before any vault code loads (the `buffer` dep was already present). And `list()` isn't queued, so App's two mount-time `list()` calls raced two clones into the same lightning-fs dir — dedupe concurrent `readFiles()` behind one in-flight promise. Any async load that can reject MUST clear the loading flag, or the UI hangs on a spinner with no error.
- **VitalsRow (S26) self-loads its own `SyncProvider`** (App mounts it propless, App.tsx out of write-set) → a second vault read, no live subscription; warmth won't reflect an elsewhere-change until remount. Contained + ADR-consistent, but **S41/S45 should wire the row to a shared subscription** when they extend it. *(distilled from 2026-07-15)*
- **Grep e2e selectors for every name a new HomeView-chain card renders, before pushing.** S27's MissionCard above NowView double-rendered the live block → pwa-e2e strict-mode found 2 elements, red. Fix is a `hideLive`-style skip prop on NowView, NOT `.first()` in the test. Same trap waits on **S32/S34/S37** — every card mounted above NowView must teach NowView what to skip. *(distilled from 2026-07-17)*
- **Global mode state (useTimeOfDay/currency/theme) that two components each instantiate can't be flipped from one without a reload** — lifting shared state needs `App.tsx` (S24's exclusive hotspot). Test the component in isolation with an override prop instead; document the gap, don't fix it inside a leaf slice. *(distilled from 2026-07-17)*
- **`gh pr merge --delete-branch` fails on `cannot delete branch … used by worktree`** when the branch is checked out in a worktree (and separately on the main checkout being on master). The branch IS merged — verify with `gh pr view <N> --json state`. Cleanup order: `git worktree remove --force <wt>` → `git branch -D <branch>` → `git worktree prune`; the merged remote branch drops via the PR merge (or `git push origin --delete`). *(distilled from 2026-07-17, re-hit 2026-07-22)*
- **After a "fix the gap" dispatch rewrites a PR branch, re-verify CI on the NEW tip, not the rolled-up status.** A rebase/amend gives the feature commit a new hash; `git push --force-with-lease=BRANCH:OLD_TIP` is the safe single-writer push (plain `--force` is the wrong reflex). Then confirm the green checks' `head_sha` matches the rebased tip (`gh api repos/.../commits/<tip>/check-runs`) — `gh pr view` rolls up the latest completed check, which may be the orphaned pre-rebase run. *(distilled from 2026-07-18)*
- **Vite inlines every `VITE_`-prefixed env var into the client bundle.** A write-scoped PAT in a public bundle = anyone can write your vault. Keep secrets runtime-only (`localStorage`), and if you must reference `import.meta.env.VITE_*` as a dev fallback, guard it with `import.meta.env.DEV` so the production minifier strips it. Verify with `grep <pat> dist/`.
- **`LiveOS-VaultRepo` is a SEPARATE private repo from the app repo**, default branch `main` (app repo is `master`). Its name is "Live" not "Life" — easy typo. Seeded headless via a `serialize.ts`-matching script; the browser/bot both read+write it over git.
- **Mutation-test every guard/race regression test before trusting it.** Strip the fix, rerun; if still green the test is vacuous (React 18 silently no-ops setState on an unmounted fiber — asserting on `result.current` after `unmount()` proves nothing). Assert on an observable side-effect *after* the guard. And "fix the flake" = root-cause across ALL sibling call-sites (grep the file for the same pattern), not just the ticket's named signature. *(distilled from 2026-07-20/#120)*
- **A review/eval subagent that dies on a transient API error (`status: failed`, no verdict) is neither pass nor reject — re-dispatch it synchronously (`run_in_background: false`) before gating.** And don't manufacture the flake you're disproving: verify the SPECIFIC failure signature (grep the exact error), don't run suites back-to-back to self-inflict concurrent load; trust CI on a clean runner. When a bundled flake fingerprint is half-fixed, amend (strike the fixed half + issue ref), don't delete the whole entry. *(distilled from 2026-07-20/#120)*
- **`GitTransport._readFiles()` hardcodes a flat `.md`-only folder allowlist** — any consumer folder not in the loop is a silent live no-op. Adding one → check `VaultSync.list()` pollution vs `parseTaskLine`'s checkbox regex first. Systemic fix tracked in #158 (recurse + non-md). *(2026-07-23)*
- **Regression tests for any `GitTransport` path MUST mock `@isomorphic-git/lightning-fs`** — the real backend's `DefaultBackend.init` touches `navigator` in an un-awaited promise that rejects post-teardown (`ReferenceError: navigator is not defined`), a local-green/CI-red unhandled-rejection that exits vitest 1 despite all tests passing. Validate config before constructing the FS. *(2026-07-23)*
- **Two grounded agents beat eleven cold ones.** The 2026-08-01 fan-out lost 11 agents and all their work to the session limit; a graph query + 2 agents got a better answer, because each prompt carried the *salvaged grounding* instead of making the agent re-derive it. Pre-load agents with verified file:line facts and name which claims to sanity-check. *(distilled from 2026-08-03)*
- **A filtered log is not evidence.** #177 sat in "investigation-required" for three days on the strength of a network panel filtered to failing rows; one unfiltered capture refuted the whole premise. When an issue reports an *asymmetry*, confirm the log wasn't filtered before theorising about the cause. *(distilled from 2026-08-03)*
- **Verify the test harness does what it claims.** `test.use({ reducedMotion })` is a silent no-op in Playwright 1.61.0 — a control experiment (`colorScheme` in the same block) is what proved it. When a test option is load-bearing for a DoD, assert the option actually took effect. *(distilled from 2026-08-03)*

## How work ships here (afk-pipeline workflow)
Plan/grill → PRD/slice issues (afk-pipeline `auto` runs this headless) → Sonnet implementer agents in isolated worktrees off fresh `origin/master` → PR → **dual-green** (CI + ponytail-review) → orchestrator merges → next slice. Label the issue `status:in-progress` **before** dispatch and let the PR close it on merge. Only pairwise-disjoint write-sets run concurrently; shared-hotspot slices serialize — checked by hand, since waves are gone.
