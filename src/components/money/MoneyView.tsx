import { Card } from '../glass/Card'

/**
 * MoneyView — Money tab: Net worth · Burn, then Portfolio · Bills radar (§5).
 *
 * STUB (S24). S40 fills it. Placeholder Card so App.tsx mounts it once and
 * never edits this file again.
 */
export function MoneyView() {
  return (
    <Card heading="Money">
      <p className="text-sm text-dim">Net worth, burn, portfolio &amp; bills land in S40.</p>
    </Card>
  )
}
