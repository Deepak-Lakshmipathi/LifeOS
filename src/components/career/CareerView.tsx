import { Card } from '../glass/Card'

/**
 * CareerView — Career tab: full-width job pipeline, then Courses (§5).
 *
 * STUB (S24). S44 fills it. Placeholder Card so App.tsx mounts it once and
 * never edits this file again.
 */
export function CareerView() {
  return (
    <Card heading="Career">
      <p className="text-sm text-dim">Job pipeline &amp; course progress land in S44.</p>
    </Card>
  )
}
