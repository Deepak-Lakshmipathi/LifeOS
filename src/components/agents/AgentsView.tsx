import { Card } from '../glass/Card'

/**
 * AgentsView — Agents tab: fleet table, then supervisor report card (§5).
 *
 * STUB (S24). Head of the AgentsView chain (S49 → S53 → S54); S49 fills it.
 * Placeholder Card so App.tsx mounts it once and never edits this file again.
 */
export function AgentsView() {
  return (
    <Card heading="Agents">
      <p className="text-sm text-dim">Fleet health &amp; supervisor report land in S49.</p>
    </Card>
  )
}
